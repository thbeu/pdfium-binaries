diff --git a/.gclient b/.gclient
index 07a5b933a..b3187e5c2 100644
--- "a/BUILD.gn"
+++ "b/BUILD.gn"
@@ -50,6 +50,7 @@ config("pdfium_common_config") {
   if (is_win) {
     # Assume UTF-8 by default to avoid code page dependencies.
     cflags += [ "/utf-8" ]
+    cflags += [ "/MD" ]
 
     if (!is_clang) {
       cflags += [
